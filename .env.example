# =============================================================
# CV ATS Builder — Environment Variables
# =============================================================
# CARA PAKAI:
# 1. Copy file ini: cp .env.example .env
# 2. Isi nilai sesuai konfigurasi kamu
# 3. Restart dev server: npm run dev
# =============================================================

# URL Web App Google Apps Script setelah di-deploy
# Cara dapat URL ini:
#   1. Buka script.google.com → pilih project
#   2. Deploy → Manage deployments → Copy URL
VITE_GAS_ENDPOINT=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
